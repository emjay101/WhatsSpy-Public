If you install WhatsSpy Public for the first time there is no need to execute anything in this directory.

In case you upgrade this isn't required either, but if the upgrade fails, you can manually upgrade using the SQL files in this directory.